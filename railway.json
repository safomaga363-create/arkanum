{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npx prisma generate && npx prisma db push --skip-generate && node server.js",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 300
  }
}
